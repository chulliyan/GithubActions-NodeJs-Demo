name: node js git hub action 
on:
  workflow_dispatch:
  push:
    branches:
      - master
jobs:
  job1:
    strategy:
      matrix:
        operating_systems: [windows-latest, ubuntu-latest]
    runs-on: ${{matrix.operating_systems}}
    steps:
      - name: checkout repo
        uses: actions/checkout@v6
      - name: setups node js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: npm
      - name: setup prerequisite
        run: npm ci
      - name: run tests
        run: npm test
